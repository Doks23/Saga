# Cleanup Report

Generated: 2026-06-20

---

## File Classification

| Path | Classification | Reason | Confidence | Suggested Action |
|---|---|---|---|---|
| `index.tsx` | Safe to keep | Main application source | High | Split into multiple files; do not delete |
| `index.css` | Safe to keep | All application styles | High | Split into modular CSS files; do not delete |
| `index.html` | Safe to keep | HTML entry point | High | Fix duplicate script tag and importmap; do not delete |
| `package.json` | Safe to keep | Dependency and script definitions | High | Add `test`, `lint`, `format` scripts; do not delete |
| `tsconfig.json` | Safe to keep | TypeScript configuration | High | Review for unused options (`experimentalDecorators`); do not delete |
| `vite.config.ts` | Safe to keep | Build configuration | High | Review for duplicate env var injection; do not delete |
| `.gitignore` | Safe to keep | Standard gitignore | High | None needed |
| `README.md` | Safe to keep | Project documentation | High | Updated |
| `metadata.json` | Review before deleting | Auto-generated by Google AI Studio | High | Likely unused by the app itself; verify if AI Studio or any external tool depends on it |
| `node_modules/` | Safe to keep | Installed dependencies | High | Already gitignored; can be regenerated |
| `dist/` | Likely safe to delete | Build output | High | Does not exist yet; would be generated by `npm run build` |

### Detailed Analysis

#### `metadata.json`

**Classification:** Review before deleting

This file contains:
```json
{
  "name": "Saga",
  "description": "Prompt Gemini in this simple example",
  "requestFramePermissions": [],
  "majorCapabilities": []
}
```

It appears to be a metadata descriptor generated by Google AI Studio when the app was exported. The description is inaccurate ("simple example") and the `majorCapabilities` array is empty. The file is not imported or referenced by any source file (`index.tsx`, `vite.config.ts`, `tsconfig.json`, `package.json`).

**Risk of deleting:** Low to Medium. It may be regenerated if the project is re-imported into AI Studio, or it may be required for AI Studio's app listing process. Without knowing the exact export process, it's safer to keep until verified.

**Action:** Review whether AI Studio requires this file. If not, it can be deleted.

---

## Branch Cleanup Review

**No git repository detected.** The directory does not contain a `.git` folder. There are no branches — local or remote — to review.

**Suggested action:** Initialize git repository:

```bash
git init
git add .
git commit -m "Initial commit: Saga AI storyteller app"
```

After initialization, adopt a branching strategy (e.g., `main` + feature branches).

---

## Git Hygiene Review

| Category | Status | Notes |
|---|---|---|
| **.gitignore** | **Good** | Covers node_modules, dist, logs, editor files. No improvement needed. |
| **.env.example** | **Missing** | No `.env.example` file. Developers must guess that `GEMINI_API_KEY` is needed. Create one. |
| **Package scripts** | **Minimal** | Only `dev`, `build`, `preview`. Missing: `test`, `lint`, `format`, `typecheck`. |
| **Lock files** | **Missing** | No `package-lock.json`, `yarn.lock`, or `pnpm-lock.yaml`. Installs are non-reproducible. Run `npm install` to generate one and commit it. |
| **Large files** | **None** | No large files. The biggest is `index.tsx` at ~71KB. Acceptable. |
| **Build artifacts** | **None** | `dist/` is gitignored and does not exist yet. |
| **Logs/temp files** | **None** | No log files present. `.gitignore` covers common patterns. |
| **Duplicate files** | **One issue** | `index.tsx` is loaded twice in `index.html` (lines 26 and 27) via duplicate `<script type="module" src="index.tsx">` tags. One should be removed. |
| **Dead code** | **Potential** | `metadata.json` is not referenced anywhere. The `process.env.API_KEY` and `process.env.GEMINI_API_KEY` are both injected in `vite.config.ts` but only `process.env.API_KEY` is used in code. The GEMINI_API_KEY injection is unused dead config. |
| **Missing tests** | **Critical gap** | Zero test files. No testing dependencies. Critical functions (`safeJsonParse`, `splitText`, `pcm16ToWavBlob`) have no tests. |
| **Missing linting/formatting** | **Gap** | No ESLint, Prettier, or equivalent. No lint scripts in `package.json`. |
| **Missing license** | **Missing** | No `LICENSE` file. Code is marked "Apache-2.0" in the `@license` header of `index.tsx`, but there is no license file in the repository root. |
| **Missing contribution guide** | **Missing** | No `CONTRIBUTING.md` or `CODE_OF_CONDUCT.md`. Acceptable for the current stage. |
| **Duplicate env var injection** | **Minor issue** | `vite.config.ts` injects both `process.env.API_KEY` and `process.env.GEMINI_API_KEY` with the same value. Only `process.env.API_KEY` is used in source code. The `GEMINI_API_KEY` variant adds unnecessary surface area. |

---

## Summary of Cleanup Candidates

### Safe to Delete (after verification)

| Item | Reason |
|---|---|
| `index.html:27` duplicate `<script>` tag | Causes double module execution |
| `process.env.GEMINI_API_KEY` in `vite.config.ts` | Unused; only `process.env.API_KEY` is used |

### Needs Verification Before Deletion

| Item | Reason |
|---|---|
| `metadata.json` | AI Studio artifact; determine if external tools depend on it |
| `experimentalDecorators` in `tsconfig.json` | Unused in source code; safe to remove but non-breaking |

### Do Not Delete

Everything else — especially `index.tsx`, `index.css`, `index.html`, `package.json`, `tsconfig.json`, `vite.config.ts`, `.gitignore`.

---

## Recommended Cleanup Order

1. Remove duplicate `<script>` tag in `index.html` (line 27) — trivial, no risk
2. Remove unused `process.env.GEMINI_API_KEY` from `vite.config.ts` — trivial
3. Run `npm install` and commit `package-lock.json`
4. Add `.env.example` file
5. Review and decide on `metadata.json`
6. Add testing framework (Vitest)
7. Add linting (ESLint) and formatting (Prettier)
8. Add `LICENSE` file (Apache-2.0 per existing header)
